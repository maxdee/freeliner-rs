extern crate freeliner;
// use freeliner::geometry::*;
// use freeliner::timer::Timer;

fn main() {
    // let mut context = Context::new();
    // context.start();
    println!(
        "welcome to freeliner-rs version {}",
        freeliner::get_version()
    );

    // let t = Timer::new();
    // while true {
    //     println!("{}", t.get_time());
    // }

    // context.
    // let point_a = Point::new(0.0, 2.0, 0.0);
    // let point_b = Point::new(3.0, 2.0, 42.0);
    // println!("{:?}", Point::lerp(&point_a, &point_b, 0.5));
}
